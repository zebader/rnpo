---
import { Image } from "astro:assets";
import raquel from "../../assets/raquel.webp";
import LetsTalkMailButton from "../LetsTalkMailButton.astro";
import LinkedInButton from "../LinkedInButton.astro";

import headerInfo from "../../data/header-info.json";
---

<header
  class="max-w-4xl m-auto py-16 md:py-32 px-8 flex flex-col-reverse  md:flex-row justify-center md:justify-between items-center gap-6"
>
  <div>
    <p
      class="header-title prose-gray font-bold text-center md:text-left"
      set:html={headerInfo.introText}
    />
    <div class="prose prose-xl py-4 text-center md:text-left">
      <h1>{headerInfo.title}</h1>
    </div>
    <div class="flex flex-col  md:flex-row gap-4 justify-center md:justify-start items-center" >
      <span class="text-3xl">👩&zwj;🚀</span>
      <p
        class="text-gray-500 text-center md:text-left"
        set:html={headerInfo.caption}
      />
    </div>
    <div class="py-4 flex justify-center md:justify-between">
      <div class="flex items-center gap-4">
        <LetsTalkMailButton />
        <div>
          <LinkedInButton />
        </div>
      </div>
    </div>
  </div>
  <div>
    <Image
      src={raquel}
      width={300}
      height={300}
      alt="Raquel Navarro product manager header"
      loading="eager"
      class="rounded-2xl w-52 md:w-[300px]"
    />
  </div>
</header>
